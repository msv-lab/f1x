#define F1X_VERSION_MAJOR @F1X_VERSION_MAJOR@
#define F1X_VERSION_MINOR @F1X_VERSION_MINOR@
#define F1X_VERSION_PATCH @F1X_VERSION_PATCH@

#define USE_GLOBAL_VARIABLES 0

// 0 - disabled
#define MAX_LOCATIONS 0
#define MAX_EXECUTIONS_PER_LOCATION 0
#define MAX_CANDIDATES_PER_LOCATION 0

enum class LocalizationHeuristic {
  OCHIAI,
  JACCARD,
  TARANTULA  
};

#define LOCALIZATION_HEURISTIC LocalizationHeuristic::JACCARD

#define ENABLE_CONDITION_PRIORITIZATION 1

enum class CandidateSelectionHeuristic {
  PREDICATE_SWITCHING, // predicate switching for failing test
  MAX_PASSING          // prioritize candidates that pass maximim number of tests
};

#define CANDIDATE_SELECTION_HEURISTIC CandidateSelectionHeuristic::PREDICATE_SWITCHING

enum class TestSelectionHeuristic {
  FIXED_ORDER,      // failing, then passing
  MAX_FAILING,      // prioritize tests that failed maximum number of times
  LARGEST_PARTITION // prioritize tests with largest average partition size
};

#define TEST_SELECTION_HEURISTIC TestSelectionHeuristic::MAX_FAILING

#define RUNTIME_COMPILER "g++"

#define PATCH_DUMMY_REPLACEMENT "%F1X_PATCH_DUMMY_REPLACEMENT%"
